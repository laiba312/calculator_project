"use strict";
/*Famous Quote 2: Repeat Exercise 4, but this time store
 the famous person’s name in a variable called famous_person.
 Then compose your message and store it in a new variable called message.
 Print your message.*/
// Declare a variable for the famous person
let famous_person = 'Albert Einstein';
// Declare a variable for the quote
let quote2 = 'A person who never made a mistake never tried anything new.';
// Compose the message
let message = `${famous_person} once said, “${quote2}”`;
// Print the message
console.log(message);
